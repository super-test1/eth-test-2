version: 1
jobs:
  patata_job:
    image: node
    script:
      - npm install truffle
      - npm install
      - ./node_modules/.bin/truffle compile
      - ./node_modules/.bin/truffle test
    branch: master
